<h1>רשימות קניה</h1>

<div class="row">
	<div class="col-md-12 bottom-space">
		<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#shoppingListFormModal">
		  צור רשימה חדשה
		</button>
	</div>
</div>

<div class="row text-center">
  <%= render @shopping_lists %>
</div>
<%= will_paginate @shopping_lists, :previous_label => 'הקודם', :next_label => 'הבא' %>


<%= render partial: "shopping_lists/form_modal", locals: { shopping_list: ShoppingList.new } %>