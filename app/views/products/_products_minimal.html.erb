<table class="table table-striped table-condensed">
	<thead>
		<tr>
			<th>שם המוצר</th>
			<th>כמות</th>
		</tr>
	</thead>
	<tbody>
		<% products.each do |product| %>
			<tr>
				<td><%= product.title %></td>
				<td><%= measure_in_words(product.measure) %></td>
			</tr>
		<% end %>
	</tbody>
</table>